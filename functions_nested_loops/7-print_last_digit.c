#include "main.h"

/**
 *Prototype: int print_last_digit(int)
 *Descripcion:function that prints the last digit of a number
 *Return:value of b
 */

int _last_digit(int)

{
        int b, r;

        if (r < 0)
        b = -1* (r % 8);
        else
	b = r % 10;

	_putchar((n % 10) + '0');
	return (n % 10);	

}


