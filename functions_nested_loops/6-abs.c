#include "main.h"

/**
 *_abs -  function that computes the absolute value of an integer
 *@b: integre input
 *Return:value of b
 */

int _abs(int b)

{

	if (b >= 0)
	return (b);

	else
	return (b * -1);
}
