#include "main.h"

/**
 *Prototype: int _abs(int)
 *Descripcion: function that computes the absolute value of an integer
 *Return:value of b
 */

int _abs(int)

{
        int b

        if (b >= 0)
        return (b);

	else
	return(r* -1);

}


