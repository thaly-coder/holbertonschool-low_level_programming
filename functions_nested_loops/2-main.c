#include "main.h"
/**
 * main - check the code.
 *
 * Return: Always 0.
 */
int main(void)
{
   	char x;
        int z;

        for (z = 0; z < 10; z++)
        {

                for (x = 'a'; x <= 'z'; x++)
                       _ putchar(x);
                _putchar('\n');
        }

    return (0);
}
